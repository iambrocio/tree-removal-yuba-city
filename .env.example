# Production origin, no trailing slash. Read at build time.
# Required for self-referencing canonical tags — without it, no canonical is
# emitted in production builds (a wrong canonical is worse than none).
NEXT_PUBLIC_SITE_URL=https://example.com
